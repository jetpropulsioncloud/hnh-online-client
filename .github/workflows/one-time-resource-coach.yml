name: resource-coach-fix
on:
  push:
    branches: [fix/fixed-hand-resources-v082]
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/fixed-hand-resources-v082
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Patch resource HUD
        run: python tools/resourcecoachfix.py
      - name: Run regression suite
        run: |
          node tests/smoke-test.js
          node tests/rules-test.js
          node tests/rules-patch-test.js
          node tests/ability-completion-test.js
          node tests/ability-edge-test.js
          node tests/ui-coordinator-test.js
          node --check data.js
          node --check engine.js
          node --check client.js
      - name: Commit resource coach
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add client.js styles.css tests/ui-coordinator-test.js
          git diff --cached --quiet && exit 0
          git commit -m "Improve resource HUD onboarding"
          git push origin HEAD:fix/fixed-hand-resources-v082
